{
  "name": "@usenami/graph-snapshot",
  "version": "0.1.0",
  "private": true,
  "description": "Reads Uniswap V3 market data from The Graph and verifies the indexer attestation (response bytes, deployment, signer, on-chain allocation). Snapshot assembly and signing are NOT implemented yet — see README.",
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "test": "node --test test/*.test.js",
    "drift": "node src/drift.js",
    "quote": "node src/quote.js",
    "drift:world": "node src/world-drift.js",
    "demo": "node src/demo.js",
    "demo:cast": "node scripts/record-cast.mjs demo.cast",
    "test:offline": "GRAPH_SNAPSHOT_OFFLINE=1 node --test test/*.test.js"
  },
  "dependencies": {
    "@x402/evm": "^2.24.0",
    "@x402/fetch": "^2.24.0",
    "viem": "^2.56.3"
  }
}
